import { track } from 'ripple';
import { Repo } from './Repo.ripple';

export component App() {
	try {
		<Repo owner="gwennlbh" name="shapemaker" />
		<Repo owner="cigaleapp" name="cigale" />
	} pending {
		{""}
	}

	<style>
		:global(body) {
			font-family: system-ui, sans-serif;
		}
	</style>
}
